// longhand
const fruits = ['mongo', 'peach', 'banana'];
for (let i = 0; i< fruits.length; i++) {}

// shorthand
// to access index in an array
for (let index in fruits) {}

// to access value of an array
for (let fruit of fruits) {}
